# Issue #24: LLM OCR の文字領域座標対応

## 目的

LLM OCR から文字領域ごとの画像ピクセル座標を取得し、既存の SSE とフロントエンド重畳表示へ渡す。

## 受け入れ条件

- 矩形付きJSON配列から複数の `TextRegion` を `positioning: available` で生成する。
- 説明文や Markdown コードフェンスを含む応答からJSON配列を抽出できる。
- テキストが妥当で座標だけが不正な要素は、`positioning: unavailable` の疑似領域として維持する。空テキストなど使えない要素だけを破棄する。
- JSON配列を解釈できた場合は空配列を含めて結果をそのまま使い、配列を解釈できない場合だけ従来の疑似領域へ安全に劣化する。
- 説明文中に無関係な配列が先行しても、文字領域の配列を選択する。
- `FallbackOcrEngine` の既存クリーニングを座標付き領域にも適用する。
- 複数の座標付き領域が SSE で配信され、フロントエンドで重畳される。

## TDD

1. LLM HTTP 層を既存の `_transport()` でモックし、構造化応答、部分的な不正要素、非JSON、範囲外、必須項目欠落、空応答のテストを先に追加した。
2. 構造化応答の2テストが、応答全体を単一の `unavailable` 領域にする既存実装に対して失敗することを確認した。
3. レビュー指摘に対し、座標不正時のテキスト維持、正常な空配列、先行する無関係な配列、画像境界に一致する矩形のテストを先に追加し、修正前に5件の失敗を確認した。
4. 応答パーサーと呼び出し側を修正し、追加・更新したLLM OCRテスト10件が成功することを確認した。
5. SSE の複数領域契約と React の複数重畳表示を回帰テストで確認した。

## 実装方針

- OpenAI 互換バックエンドごとの対応差を避けるため `response_format` は追加せず、プロンプト指示と応答パースで対応する。
- パース処理は既に300行を超えている `ocr.py` から `llm_ocr_response.py` へ分離する。
- 座標は送信画像の左上を `(0, 0)` とする。小数値は領域を欠けさせないよう外側の整数ピクセル境界へ広げる。
- 画像範囲外、非数値、幅・高さが0以下、座標項目欠落の要素は、テキストを保った `unavailable` の疑似領域へ変換する。空テキストなどテキスト自体が不正な要素だけを破棄する。
- JSON配列の復号成否と領域一覧を分けて返し、正常な空配列とパース失敗を区別する。
- 複数の配列候補がある場合、`text` キーを持つ辞書要素を含む配列を優先する。

## 対象外

- 新しい LLM プロバイダーやモデルの追加。
- Tesseract 側の変更。
- プロンプトチューニングによるOCR精度改善。
- API固有のJSONモードの利用。

## 実モデル評価

実行コマンド、今回実行できなかった理由、後続の人手確認項目を [`verification/2026-08-10_llm_ocr_positioning/README.md`](../verification/2026-08-10_llm_ocr_positioning/README.md) に記録した。

## 検証結果

- LLM OCRテスト10件: 成功。
- 実ソケット3モジュールと既知の `TestClient` 2モジュールを除く Python 回帰テスト175件: 成功。
- Python 全テストコマンド: 実行したが、サンドボックスのソケット生成禁止により既存12件がエラーとなり、その後既知の `TestClient` テストで停止したため中断した。
- `PYTHONPATH=src .venv/bin/mypy src`: 成功。
- `PYTHONPATH=src .venv/bin/mypy --platform win32 src`: 成功。
- `RUFF_CACHE_DIR=/tmp/ruff_cache .venv/bin/ruff check src`: 成功。
- フロントエンド `npm run lint`: 成功。
- フロントエンド `npm test`: 7ファイル、58件成功。
- フロントエンド `npm run build`: 成功。
